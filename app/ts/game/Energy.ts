class Energy extends GameEntity {

    constructor (x, y) {
        super(x, y, '#303030');
    }

    beginTick(): void {
    }

    updateTick(ts: number, percent: number): void {
    }

    endTick(): void {
    }

    update(ts: number) {
    }
}